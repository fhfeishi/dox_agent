# 审核报告：plan.md / ui_optimization.md 对照 demand.md（2026-09-21）

- 审核对象：[`plan.md`](plan.md)、[`ui_optimization.md`](ui_optimization.md)
- 审核基准：[`../demand.md`](../demand.md)（含 2026-09-21 追加的 §9.5–§9.7 与 §8.2 同步）
- 辅助材料：[`../status.md`](../status.md)、[`../query.md`](../query.md)、[`../design/`](../design/README.md)
- 审核方式：文档逐条比对 + 源码抽样核对（`src/main.py`、`src/knowledge.py`、`src/agent/graph.py`、`src/agent/routing.py`）+ `git log`

## 0. 总体结论

**不通过（需修订后再执行）。** 两份文档本身质量较高：技术描述与源码基本一致，上一轮 14 条审核意见已闭环（`plan.md`「审核意见采纳」表）。但差距集中在**最新一批 demand 变更未同步到 plan.md**：

- `demand.md` §9.5–§9.7（侧栏收束、文献库一级入口、模型选择分期、科研头条）与 §8.2 新增三条验收，在 `ui_optimization.md` 已落成 §9（U9.1–U9.4，25 处引用），但在 `plan.md` 中**零覆盖**（`grep -E "U9|文献库|收束|/api/models|/api/news|科研头条|模型选择" plan/plan.md` 无任何命中）。
- `ui_optimization.md` 文件头声明 U5–U8 已实施，但 §2 现状表仍把这四项标为「⬜ 待处理」，同一文件内部自相矛盾。

`plan.md` 是任务拆解与完成情况记录，缺任务即意味着 §9.5–§9.7 不会被排期执行。建议按下文清单修订后再进入实施。

---

## 1. P0 · 阻断项

### P0-1　`plan.md` 完全未同步 demand §9.5–§9.7

- demand 已在 §1（核心交付第 3 项）、§2.2（4 行目标差距）、§6（2 条拟议接口）、§8.2（3 条验收）中引用这批要求，属**现行需求基线**。
- `ui_optimization.md` §9 已给出 U9.1–U9.4 的可执行设计，但 `plan.md`：
  - 「阶段总览」（§阶段总览）无对应阶段行；
  - 「U. UI 优化」表止于 U8（U 节最后一项为 U8，line 296），无 U9.1/U9.2/U9.4-第一步；
  - 设计指南 §1「定位与职责」、§3「本地文档预览」只写了右上「本地文档」入口，未含 demand §9.5 要求的**左栏「文献库」一级入口**。
- 影响：`ui_optimization.md` §9 中的「无后端依赖、可在 U1 之前执行」的 U9.1/U9.2/U9.4-第一步没有进入 plan 的排期与完成记录，无法跟踪。

**建议**：plan.md 新增

| 编号 | 任务 | 依赖 |
|---|---|---|
| U9.1 | 侧栏收束/展开（图标栏 ↔ 全宽，`localStorage` 持久化，键盘可达） | 无 |
| U9.2 | 「文献库」一级入口：卡片/列表视图、按名称筛选、点击复用 `DocumentPreview` | 无（复用 `useDocuments`） |
| U9.3 | 科研头条占位（`VITE_UI_NEWS` 默认 off，不实现抓取） | 后端 `GET /api/news`（未定） |
| U9.4-1 | 设置抽屉只读展示当前模型 | 无 |
| U9.4-2 | 模型选择器（`VITE_UI_MODELS` 默认 off，请求可选 `model`） | 后端 `GET /api/models`（未定） |

并在「阶段总览」补阶段行、在「完成情况记录」预留对应记录位。

### P0-2　`ui_optimization.md` §2 现状表状态自相矛盾

- 文件头（第 3 行）：「U0（U0.1–U0.8）、U4.1–U4.2 与 **U5–U8 已实施并离线验收**」。
- §3 后端依赖矩阵、§8 各节亦标 ✅ U5/U6/U7/U8。
- 但 §2「现状与问题（已核对）」表末四行仍标 **⬜**：
  - `编辑并重问新增独立会话 … ⬜ U5`
  - `"断开连接并退出"占整行 … ⬜ U6`
  - `无法预览知识库正文 … ⬜ U7`
  - `LLM/状态不在左下角 … ⬜ U8`

- 事实核对：`git log` 最新提交 `ce3b4e7 U7 knowledge preview + U6 power button + U8 LLM status`、`9a0c899 U5: edit-and-reask branches in place`；`frontend/src/` 下 `branches.ts`、`DocumentPanel.tsx`、`DocumentPreview.tsx`、`documentPreview.ts` 均存在。即 **U5–U8 确已实施**，§2 表是过期数据。

**建议**：把 §2 这四行状态改为 ✅ U5/U6/U7/U8（措辞与同表前段「✅ 已解决（U0，提交 72fde7a）」统一）。

---

## 2. P1 · 重要项

### P1-1　F 节（首期验收）未随 demand §8.2 更新

demand §8.2 新增三条验收，plan.md F1–F8 均无对应项：

| demand §8.2 新增验收 | plan 覆盖 |
|---|---|
| 左侧边栏可收束/展开，状态可保持；收束后新对话/文献库/设置仍可达，异常提示不隐藏 | ❌ |
| 文献库入口展示的文档与 `GET /api/documents` 一致，点击可预览；浏览不改变检索范围 | ❌ |
| 界面如实显示当前模型；模型不可用时给出明确提示，不静默切换 | ❌（仅 U8 做展示） |

**建议**：F 节追加 F9/F10/F11。

### P1-2　「首期不做」与 demand §8.2 不同步

- demand §8.2（line 224）明列：`外部资讯抓取与科研头条`、`多模型路由与模型管理后台`。
- plan.md「首期不做」（line 300–302）列了 10 项，**缺上述 2 项**（其余「多租户/多 Agent/知识图谱/工作流画布/独立分类服务/材料遵循等级/自动全网研究/自动订阅同步/模板管理平台/分布式任务队列」均一致）。

**建议**：补齐 2 项，保持逐项对齐。

### P1-3　plan.md §5「目标接口（增量总览）」缺 2 条拟议接口

- demand §6 新增 `GET /api/models`（拟议，line 174）、`GET /api/news`（拟议，line 175），并明确「未实现前界面不发请求」。
- plan.md §5（line 163–173）仅含 tasks/chat/documents/file/workspace/reports，**无这两条**。

**建议**：§5 补两行并标注「拟议、未定，首期不实现；前端默认关闭」。

### P1-4　「模型不可用」提示无任务承载，且现无判定依据

- demand §9.6（line 280）与 §8.2（line 222）都要求「模型不可用 → 明确提示、不静默切换」。
- plan.md U8（line 296）与 ui_optimization §8 U8（line 324）的红灯条件为「断连 / 未配置密钥 / health 失败 / `preparation=error`」，**不含「模型本身不可用」**（模型名失效、provider 不可达、鉴权失败）。
- 源码核对：`GET /api/health` 硬编码 `"model_verified": False`（`src/main.py:108`），当前无任何逻辑消费该字段，前端也拿不到可判定的「模型可用性」信号。

**建议**：明确「模型可用性」的判定来源（health 增可判定字段或新增探测），并把它列入 U8 的红灯/提示逻辑与 F 节验收；否则该需求长期悬空。

### P1-5　ui_optimization.md §8 标题与实施状态矛盾

- §8 标题为「U0 实机反馈修订（**拟议**，2026-09-21 17:26）」，U5–U8 各节仍以「目标/设计/验收」的将来时书写（如 line 230「目标：编辑与重问合并进原会话」）。
- 但文件头与 §3 已标 U5–U8 已实施；`plan.md` 完成情况记录亦有 U5/U7/U6-U8 的验收证据行。

**建议**：§8 标题改为「U0 实机反馈修订（**已实施并离线验收**，2026-09-21 17:26）」，或在文首统一加一句「本文件只保留方案与依赖关系，实施状态以 `plan.md` 为准」（该约定已写在 §8 末尾，建议提前到文件头）。

### P1-6　会话悬停菜单三处不一致（复制 / 删除混用）

| 位置 | 表述 |
|---|---|
| §4 会话栏优化（line 158） | 悬停菜单：重命名 / 归档 / **复制**；括号说明却是「**"删除"**为补充项」 |
| G6（line 269） | 会话栏重构：…重命名/归档/**删除** |
| U0.5（line 116） | 悬停菜单（重命名/归档，**不含复制**） |

三处菜单项集合不同，且「复制」与「删除」混用。demand §9.1 只要求「重命名、归档/恢复、分支与任务标签」，二者均为超出范围的补充项。

**建议**：统一为「重命名 / 归档」，并把「复制」「删除」移到 §4/§6 的补充项说明中并列明「未纳入首期、需确认」。

### P1-7　测试基线数字过期

- `ui_optimization.md` §1.1（line 16）：`node --test src/` → **12 passed**（Node v26.8.1）。
- `plan.md` 完成情况记录：U0 时 `npm test` 12 passed，**U5 后 15 passed**（U5 行）。
- `status.md` §6 仅记 12 passed。

**建议**：统一为当前值（15 passed），或在括号注明「U0 基线 12，U5 新增用例后 15」。

### P1-8　提交号记录口径不统一

- `ui_optimization.md` 写「phase C 提交 `0ff1480`」「U0 提交 `72fde7a`」；`query.md` 写「`be1ddc5..72fde7a`」。
- `plan.md` 完成情况记录**只写日期，不写提交号**，导致 U5–U8 的证据无法按提交回溯。

**建议**：在 plan.md 完成情况记录中为每条补提交号（U5 → `9a0c899`，U6/U7/U8 → `ce3b4e7`），并统一三份文档的提交号写法。

---

## 3. P2 · 建议项

- **P2-1**　`plan.md` §2.1 任务表的「对应需求」列未引 §9.3（现写 task1/2→§3，task3→§4.3，task4→§4）。任务系统的正式出处是 demand §9.3，建议补注。
- **P2-2**　`ui_optimization.md` U9.2 卡片字段略超 demand §9.5（demand 仅要求标题/页数/采集时间；ui 增加「文件份数」「大小」）。已注明「以现有 `DocumentInfo` 字段为准」，建议显式标为可选，避免被当作硬要求。
- **P2-3**　仓库内 `src/prompts/` 目录**已存在但为空**，而 plan G1 标 ⬜「建立 `src/prompts/`」。空目录不被 git 跟踪，易被误读为「已开始」。建议 G1 注明「目录占位、提示词文件未建」或删除空目录。
- **P2-4**　`status.md` §5「尚未实现」未列 §9.5–§9.7 相关项（侧栏收束、文献库、模型选择器、科研头条），§6 未补 U5–U8 验证行。建议同步，避免「现状基准」落后于 plan。
- **P2-5**　`plan.md` 阶段总览 A/C 标 🟡，与 demand 开头「阶段 A 与阶段 C 已实施并验证」口径不同。建议加脚注说明 🟡 系 A7/C4 收尾项所致，或统一口径。

---

## 4. 已核对一致（正面清单）

以下经逐条比对确认无问题，可作为基线保留：

1. chat 契约：demand §6 要求「task_id（task1–3）+ filters，不接受 task4」，plan §2.4/§2.5/G3、ui §3 一致；`filters` 归 B5、`task_id` 归 G3 的分工明确。
2. task4 单入口：chat 不生成正文、统一走 `POST /api/reports`（demand §9.3、plan §2.4/§2.5/G3/G9、ui U1.3/U3）一致。
3. 任务边界：task3 聊天式趋势、task4 落盘报告（demand §9.3、plan §2.1 line 57、ui §2 大纲）一致。
4. 移除策略字段：`query_routing`/`evidence_level`/`execution_mode` 由 task 取代（demand §2.2/§9.3、plan C1/C2/C3、ui §2）一致；且与源码一致（`ChatRequest` 仅 `messages`/`allowed_doc_ids`/`run_id`，`extra="forbid"`，`src/main.py:33-37`）。
5. 「浏览不改变检索范围」（demand §5/§8.2、plan D9/U2.5、ui U2.5/U7/U9.2）三处一致。
6. 超范围项标注：会话内切换任务、删除会话已标为「补充项，超出 demand §9.1」（plan §2.2/§4/审核意见 #10、ui §4/U1 注、§6）——与 demand §9.1 冻结范围一致。
7. 文献库数据源复用 `GET /api/documents`、不新增接口（demand §9.5、ui U9.2）一致。
8. 源码级事实抽查通过：
   - `/api/documents` 返回 `pages`（页数）与载荷字段（含 `kind`、`captured_at`），`src/main.py:114-117`、`src/knowledge.py:25/88-93/203`；
   - `/api/health` 返回 `model`，`src/main.py:105`；
   - `sources` 带 `citation: i+1`，`src/agent/graph.py:302`（ui U2.4 所述「graph.py:304」误差 2 行，可忽略）；
   - `stop_reason="professional"`，`src/agent/routing.py:31`（ui 所述 U0.6 B1 修复依据成立）；
   - 全局 `answer_policy` 允许「有依据的推导」，`src/agent/routing.py:35`（证实 plan 待定设计 #11 的矛盾确实存在，需按既定方案在 G1/G4 定稿）。

---

## 5. 建议的修订顺序（可直接执行的清单）

1. `plan.md`：新增 U9.1/U9.2/U9.3/U9.4-1/U9.4-2 任务行 + 阶段总览补行（P0-1）。
2. `plan.md`：F 节补 F9/F10/F11（P1-1）；「首期不做」补 2 项（P1-2）；§5 补 2 条拟议接口（P1-3）。
3. `plan.md`：统一会话悬停菜单口径（P1-6）；补提交号（P1-8）；§2.1 补 §9.3 引用（P2-1）。
4. `ui_optimization.md`：§2 表四行状态改 ✅（P0-2）；§8 标题与文件头统一口径（P1-5）；测试基线数字更新（P1-7）。
5. 单独立项：明确「模型不可用」判定与提示（P1-4，建议落到 U8 与 F 节，需后端配合）。
6. `status.md`：§5 补 §9.5–§9.7 待办、§6 补 U5–U8 验证行（P2-4）。

> 修订完成后，建议沿用 `plan.md`「审核意见采纳」表的格式，为本报告登记采纳/未采纳结论，保持审核闭环。

## 6. 处理记录（2026-09-21 修订完成）

本报告 P0–P2 共 15 条**全部采纳并回写**，无未采纳项；采纳表登记在 [`plan.md`](plan.md) 的「第二轮审核采纳（2026-09-21）」#15–#29。复核结论：

- P0-1 成立：`grep` plan.md 原无 U9 命中 → 已补 U9.1/U9.2/U9.3/U9.4-1/U9.4-2 任务行。
- P0-2 成立：提交 `ce3b4e7`（U6/U7/U8）与 `9a0c899`（U5）实测存在 → `ui_optimization.md` §2 末四行状态改为 ✅。
- P1-4 成立：`src/main.py:108` `model_verified` 恒 `False`，仅被 `tests/test_professional_policy.py:150` 断言其值，无生产消费 → 新增 plan 待定设计 #12 承接，并作为 U9.4-2 与 F11 的前置。
- P2-3 成立：`src/prompts/` 为空目录且 `git ls-files` 无记录 → G1 行已标注「本任务从零开始」。
- 提交号归因经 `git diff-tree` 逐笔核对（phase C → `0ff1480`、U0 → `72fde7a`、U5 → `9a0c899`、U6/U7/U8 → `ce3b4e7`）。
